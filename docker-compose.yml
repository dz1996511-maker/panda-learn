version: "3.9"

services:
  panda-learn:
    build: .
    container_name: panda-learn
    ports:
      - "${PORT:-5000}:5000"
    volumes:
      - ./data:/app/data
      - ./data/uploads:/app/data/uploads
    env_file:
      - .env
    environment:
      - HOST=0.0.0.0
    restart: unless-stopped
